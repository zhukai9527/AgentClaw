{
  "name": "@agentclaw/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build:sidecar": "bun run build-sidecar.ts",
    "build:desktop": "bun run build-sidecar.ts && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
